{
  "name": "voice-forge",
  "version": "0.1.0",
  "description": "Text to speech with its dials on the outside.",
  "private": true,
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "check": "tsc && node out/check/run.js",
    "build": "tsc",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder",
    "parity": "tsc && node out/check/engine-parity.js"
  },
  "dependencies": {
    "onnxruntime-node": "^1.29.0",
    "piper-phonemize": "^1.4.11"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}